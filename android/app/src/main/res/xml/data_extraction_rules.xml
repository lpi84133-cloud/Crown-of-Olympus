<?xml version="1.0" encoding="utf-8"?>
<!--
    Explicitly opt out of cloud backup and device-transfer for this app.
    The gray shell keeps a one-time push URL and cached verdict URL in
    secure storage; those are user- and session-specific and should not
    survive a device migration or Google backup restore.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
